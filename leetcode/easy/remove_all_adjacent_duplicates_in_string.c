#include <stdio.h>
#include <string.h>

char *removeDuplicates(char *s);

int main(void) {
    char *s = "sdasczaa";
    printf("%s", s);

    removeDuplicates(s);
}

char *removeDuplicates(char *s) {
    char *p1 = NULL, *p2 = NULL;
    char solution[strlen(s)];

    p1 = s;
    p2=s;
    while (p1 != NULL) {

    }
}
